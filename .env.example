# Git Configuration
# Comma-separated list of repository paths to monitor
REPOS=/path/to/repo1,/path/to/repo2,/path/to/repo3

# Optional: Filter commits by author (leave empty for all authors)
GIT_AUTHOR=Your Name

# LLM Configuration
# OpenAI API key (get from https://platform.openai.com/api-keys)
OPENAI_API_KEY=sk-...

# Model to use (gpt-4-turbo-preview, gpt-4, gpt-3.5-turbo)
LLM_MODEL=gpt-4-turbo-preview

# API Configuration
# Backend API endpoint for storing timeline articles
API_ENDPOINT=https://your-api.com

# API authentication key
API_KEY=your-api-key-here

# Optional Configuration
# Time period (Morning, Afternoon, Evening, Night) - auto-detected if not set
PERIOD=

# Logging level (debug, info, warn, error)
LOG_LEVEL=info

# MongoDB connection string (for backend server)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/timeline?retryWrites=true&w=majority

# Server port (for backend API)
PORT=3000
